# RentalManager v0.2.2

- Adds Velopack-based GitHub Releases update checks for installed builds.
- Shows an update prompt only when a newer Velopack release is available.
- Keeps the existing zip release flow unchanged.
- Keeps local SQLite data under the existing local app data path.
